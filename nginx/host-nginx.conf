# 添加到宿主机 nginx 配置中
# CentOS 通常在 /etc/nginx/conf.d/ 下新建此文件

server {
    listen 80;
    server_name s.pmlab.top;

    # Certbot 验证
    location /.well-known/acme-challenge/ {
        root /usr/local/htdoc/stone/web;
    }

    # 前端 + API 全部反代到 Docker
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 申请证书后再加上这个 HTTPS server:
# server {
#     listen 443 ssl;
#     server_name s.pmlab.top;
#
#     ssl_certificate     /etc/letsencrypt/live/s.pmlab.top/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/s.pmlab.top/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
