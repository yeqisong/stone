# 01 — 系统架构

> K道（Stone）· A 股个股买卖点监测系统
> 文档基准：代码版本 v3.2（2026-08-16 重构整理）

---

## 1. 系统定位

全栈 A 股量化监测平台，覆盖：**多数据源行情采集 → 数据质量监控 → KEPL 特征引擎 → XGBoost 模型训练与信号 → 持仓/飞书 AI 助手**。生产部署于 https://s.pmlab.top。

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Python 3.11 · FastAPI · SQLAlchemy（原生 SQL，无 ORM 模型） |
| 数据库 | PostgreSQL 15（asyncpg 异步 + psycopg2 同步双引擎）· Redis 7 |
| 数据源 | **TuShare 唯一主源**（限流 50次/分、8000次/天，配额表跟踪）· **Baostock 字段补充器**（ROE/营收净利、ETF 复权、交易日历）。AKShare 已于 v3.2 移除 |
| 量化 | XGBoost · Optuna(TPE) · pandas/numpy（无 TA-Lib，指标纯实现） |
| 表达式 | KEPL（Lark LALR(1) 解析器，见 02 文档 §5） |
| AI | DeepSeek（OpenAI 兼容，8 个 Function Calling 工具）· 飞书 Bot |
| 前端 | Vue 3 + Vite 5 + Pinia 3 + Naive UI + ECharts + Vue Flow + Monaco Editor（详见 04 文档） |
| 部署 | Docker Compose（app + db + redis）· 宿主机 Nginx 反代 + certbot HTTPS |

## 3. 整体架构图

```
┌─────────────────────────── 生产环境 ───────────────────────────┐
│  浏览器 (https://s.pmlab.top)     飞书客户端                     │
│      │ Vue SPA (web-v2/dist)          │ 事件回调               │
│      ▼                                 ▼                       │
│  Nginx ──反代──► FastAPI (:8000)  ◄── POST /webhook/feishu     │
│                   │                                              │
│  ┌────────────────┼───────────────────────────────────┐        │
│  │  REST API (15 routers)   WS /api/ws/dag 实时推送    │        │
│  │  JWT 单用户认证           cron 调度器 (60s 扫描)     │        │
│  │  TaskManager 统一任务     DAG 流程执行引擎            │        │
│  └───────┬──────────────────────┬─────────────────────┘        │
│          ▼                      ▼                              │
│   PostgreSQL 15 (:5432)   Redis 7 (:6379)                      │
│   42 张表 · daily_quote ~1400万行                              │
└─────────────────────────────────────────────────────────────────┘
           ▲
           │ 采集（同步引擎 + 后台线程）
   ┌───────┴────────────────────────────┐
   │ TuShare API ──失败/限流──► Baostock │  (DataSourceManager 优先级 fallback)
   └────────────────────────────────────┘
```

## 4. 目录结构

```
stone/
├── app/                    # FastAPI 后端
│   ├── main.py             # 入口：lifespan + 路由注册 + 登录/健康检查
│   ├── config.py           # pydantic-settings（.env）
│   ├── db/                 # connection.py 双引擎 · schema.py 42表DDL+init_db迁移
│   ├── api/                # 15 个 REST 路由模块（见 03 文档）
│   ├── auth/               # JWT 单用户（HS256）
│   ├── task/               # TaskManager 统一任务机制
│   ├── signal.py           # 后台线程 ↔ WS 广播协程的解耦桥梁
│   ├── ai/                 # DeepSeek 客户端 + 8 个 FC 工具
│   ├── feishu/             # 飞书 Bot webhook/对话/确认卡片
│   └── kepl/               # KEPL 表达式解析器（Lark）
├── crawler/                # 数据采集层
│   ├── adapters/           # DataSourceAdapter 抽象 + tushare/baostock + Manager
│   ├── backfill.py         # BackfillManager 历史补数（按交易日补数/配额预算/断点续传）
│   ├── trade_calendar.py   # 交易日历同步
│   └── writers.py          # 批量 UPSERT（kline/index/fundamentals）
├── scripts/                # 调度与计算层
│   ├── pipeline.py         # ~2100行：全部 dag_task_* 节点 + 模型训练 + 统计
│   ├── dag.py              # DagExecutor 计划-执行模型（拓扑分层并行）
│   ├── feature_compute.py  # KEPL→pandas 特征计算引擎（COPY 流式写入）
│   └── cron_scheduler.py   # cron 定时触发（last_run_at 幂等）
├── strategy/indicators.py  # 纯 numpy/pandas 技术指标（RSI/BOLL/MACD/ATR/OBV...）
├── web-v2/                 # Vue 3 前端（见 04 文档）
├── tests/                  # pytest（unit/integration/e2e）
├── design/                 # 本设计文档
└── docs/                   # 三方库笔记（vue3/naive-ui...）
```

## 5. 启动流程（app/main.py lifespan）

```
1. APP_SECRET_KEY 校验（prod <16字符拒启）
2. init_db(db)            → 建表 + 幂等迁移 + 预置数据（16内置函数/23系统特征/策略配置）
3. entity_stats 基线      → 空表时 JOIN stock_master×trade_calendar 计算（~40s，非致命）
4. 恢复异常模型           → model_versions: TRAINING→DRAFT
5. signal.set_main_loop() → 注册 asyncio 主循环（供后台线程唤醒 WS）
6. broadcast_dag_status() → WS 广播协程（事件驱动 + 2s/30s 超时兜底）
7. start_cron_scheduler() → cron 后台线程
关闭时：cancel 广播 + dispose 异步引擎
```

## 6. 数据库表总览（42 张，DDL 见 app/db/schema.py）

| 域 | 表 |
|---|---|
| **采集** | trade_calendar · stock_master(复合PK: code+type) · daily_quote · index_daily_quote · stock_fundamentals(PK: code+trade_date) · stock_fundamentals_history · stock_industry · corporate_actions · failed_downloads · download_history |
| **扩展数据**¹ | stock_top_list · stock_moneyflow · stock_hk_hold · stock_margin_detail · stock_holder_number |
| **特征/函数** | features（KEPL公式+质量统计） · feature_values(长表, COPY写入) · functions · function_versions |
| **DAG** | dag_flows(含 cron_expr/last_run_at) · dag_flow_versions · dag_config(节点注册表) · dag_run_log · backfill_tasks |
| **统计** | daily_completeness · data_stats_cache · entity_stats · system_metrics |
| **模型** | model_versions · training_trials · version_comparisons · model_health · backtest_records · backtest_trades · strategy_scan_tasks |
| **业务** | portfolio · portfolio_history · signal_history · strategy_config · strategy_param_log · stock_treemap_cache |

> ¹ 扩展数据表 + TuShareAdapter.fetch_* 方法已就绪，但采集链路尚未接入 DAG 节点。

**迁移机制**：无 Alembic，init_db 纯幂等 SQL（建表循环 + ADD COLUMN IF NOT EXISTS 系列 + 数据修复迁移 + 预置数据），吞 "already exists" 类错误前先 rollback 避免事务 aborted。

## 7. 认证与安全

- **JWT 单用户**：`POST /api/login`（限 5 次/分/IP）→ HS256 token（24h），前端 Axios 拦截器注入 `Authorization: Bearer`。
- **写操作端点**用 `Depends(get_current_user)`；`optional_auth` 实际同样强制。
- **WS 认证**：`?token=` query 参数（prod 强制校验，dev 放行）。
- **函数沙箱**：AST 安全扫描（封 eval/exec/getattr/__class__ 等 22 个危险名 + 递归属性链 + import）+ 子进程 `python -I` 隔离模式执行。
- **已知待办**：backfill/ai-chat 等部分端点无认证；WS token 走 query 会进访问日志。

## 8. 部署要点（详见 AGENTS.md Deployment 节）

- 后端代码构建进镜像（app/crawler/scripts/strategy + Dockerfile）；`web-v2/dist` volume 挂载 rsync 即可。
- 部署流程：本地测试 → 打 tag → rsync 前端/后端 → 备份数据库 → `docker compose build --no-cache app && up -d` → 验证 /health。
- 禁止：SSH 直接改代码、未打 tag 部署、未备份迁移、覆盖生产 .env。

## 9. 已知代码实况（与历史文档口径的差异）

1. **数据源架构（v3.2.1）**：TuShare 唯一主源（按交易日全市场拉取，配额表 tushare_quota 跟踪 8000次/天）；Baostock 降级为字段补充器（ROE/营收净利、ETF 后复权）+ 交易日历唯一同步源；AKShare 已删除。无多源 fallback——tushare 不可用即任务失败。
2. **采集路径统一**：DAG 节点与补数均走 tushare 适配器按日期拉（停牌股自然缺失、退市股历史天然覆盖），BaostockCrawler 高速路径/catch_up/progress.py 已删除。
3. `dag_task_model_train` 不在 NODE_FN_MAP，仅由模型 API 触发；且固定取最新 DRAFT（多 DRAFT 并发会串版本）。
4. model_signal 预测模式要求 5d/10d/20d 三个模型文件，训练仅产出 10d/20d → 实际长期运行规则评分模式。
5. `functions.py` 存在 DELETE 路由装饰器堆叠 bug（list_versions 遮蔽 delete_function）。
6. 旧"每日固定 DAG 主流程"已下线，现行机制 = dag_flows 动态编排 + cron 定时触发；daily_crawl.sh 已改为触发已发布流程。
